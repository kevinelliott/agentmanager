name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

env:
  GO_VERSION: '1.24'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libayatana-appindicator3-dev

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest
          args: --timeout=5m

  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go-version: ['1.23', '1.24']
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}

      - name: Get dependencies
        run: go mod download

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libayatana-appindicator3-dev

      - name: Run tests
        shell: bash
        run: go test -race -coverprofile=coverage.txt -covermode=atomic ./...

      - name: Upload coverage to Codecov
        if: matrix.os == 'ubuntu-latest' && matrix.go-version == '1.24'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.txt
          flags: unittests
          name: codecov-umbrella

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    needs: [lint, test]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build CLI (Unix)
        if: matrix.os != 'windows-latest'
        run: go build -v -o bin/agentmgr ./cmd/agentmgr

      - name: Build Helper (Unix)
        if: matrix.os != 'windows-latest'
        run: go build -v -o bin/agentmgr-helper ./cmd/agentmgr-helper

      - name: Build CLI (Windows)
        if: matrix.os == 'windows-latest'
        run: go build -v -o bin/agentmgr.exe ./cmd/agentmgr

      - name: Build Helper (Windows)
        if: matrix.os == 'windows-latest'
        run: go build -v -o bin/agentmgr-helper.exe ./cmd/agentmgr-helper

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: agentmgr-${{ matrix.os }}
          path: bin/

  integration:
    name: Integration Tests
    runs-on: ${{ matrix.os }}
    needs: [build]
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build binaries (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          go build -o bin/agentmgr ./cmd/agentmgr
          chmod +x bin/agentmgr

      - name: Build binaries (Windows)
        if: matrix.os == 'windows-latest'
        run: go build -o bin/agentmgr.exe ./cmd/agentmgr

      - name: Test CLI version (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr version

      - name: Test CLI version (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe version

      - name: Test CLI help (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr --help

      - name: Test CLI help (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe --help

      - name: Test doctor command (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr doctor || true

      - name: Test doctor command (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe doctor
        continue-on-error: true

      - name: Test catalog list (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr catalog list

      - name: Test catalog list (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe catalog list

      - name: Test agent list (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr agent list || true

      - name: Test agent list (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe agent list
        continue-on-error: true

      - name: Test completion generation (Unix)
        if: matrix.os != 'windows-latest'
        run: |
          ./bin/agentmgr completion bash > /dev/null
          ./bin/agentmgr completion zsh > /dev/null
          ./bin/agentmgr completion fish > /dev/null

      - name: Test completion generation (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe completion powershell | Out-Null

      - name: Test config show (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr config show

      - name: Test config show (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe config show

      - name: Test upgrade check (Unix)
        if: matrix.os != 'windows-latest'
        run: ./bin/agentmgr upgrade --check || true

      - name: Test upgrade check (Windows)
        if: matrix.os == 'windows-latest'
        run: ./bin/agentmgr.exe upgrade --check
        continue-on-error: true

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Run Gosec Security Scanner
        uses: securego/gosec@master
        with:
          args: '-no-fail -fmt sarif -out results.sarif ./...'

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
